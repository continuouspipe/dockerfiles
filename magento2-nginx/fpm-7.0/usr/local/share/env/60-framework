#!/bin/bash

export MAGENTO_ADMIN_IP_WHITELIST=${MAGENTO_ADMIN_IP_WHITELIST:-$AUTH_IP_WHITELIST}

export BUILD_DEVELOPMENT_MODE=${BUILD_DEVELOPMENT_MODE:-$DEVELOPMENT_MODE}
export BUILD_PRODUCTION_ENVIRONMENT=${BUILD_PRODUCTION_ENVIRONMENT:-$PRODUCTION_ENVIRONMENT}
export BUILD_MAGENTO_MODE=${BUILD_MAGENTO_MODE:-$MAGENTO_MODE}
